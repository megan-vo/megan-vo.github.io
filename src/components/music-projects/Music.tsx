import React from 'react';
import { Section } from '../../styles/styles';

export const MusicProjects = (): JSX.Element => {
  return (
    <Section>
      <h2 className='section-title'>Musical Fun</h2>
    </Section>
  );
};
